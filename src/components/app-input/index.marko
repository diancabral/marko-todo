<input.input type="text" placeholder=input.placeholder minlength=input.minlength on-keydown('emitValue') required />

class {

    emitValue(e){

        if(e.key === 'Enter' && e.target.value){

            this.emit('enter', e.target.value)

            if(e.target.value.length > 3) setTimeout(() => e.target.value = '')

        }

    }

}

style {

    @import '@/scss/components.scss';

    .input {

        position: relative;

        width: 100%;

        padding: $theme-padding;

        font-family: 'Quicksand', sans-serif;
        @include font-sizer(16);

        border-radius: 6px;
        box-shadow: 0 0 20px rgba(black, .1);

        z-index: 1;

    }

}
